apiVersion: v1
kind: Service

metadata:
  name: notification-service
  labels:
    app: notification

spec:
  type: NodePort
  selector:
    app: notification
  ports:
    - protocol: TCP
      port: 80
      targetPort: 9002